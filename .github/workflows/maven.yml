name: "Backend Build and Security Scan"

on:
  push:
    branches:
      - "development"
      - "main"
    paths:
      - "**"
      - .github/workflows/maven.yml

  pull_request:
    branches:
      - "development"
      - "main"
    paths:
      - "**"
      - .github/workflows/maven.yml

  schedule:
    - cron: "0 0 * * 0"  # Runs every Sunday at midnight

permissions:
  contents: write
  actions: read

jobs:
  backend:
    name: Build and Security Scan
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Step 2: Debug - List Repository Contents
      - name: Debug - List Repository Contents
        run: ls -R

      # Step 3: Set up Java 17 (Amazon Corretto)
      - name: Set up JDK 17 (Corretto)
        uses: actions/setup-java@v3
        with:
          java-version: 17
          distribution: 'corretto'
          cache: maven

      # Step 4: Grant execute permission for Maven Wrapper
      - name: Grant execute permission for Maven wrapper
        run: chmod +x ./mvnw

      # Step 5: Build the Java Project with Maven Wrapper
      - name: Build Java Code
        run: |
          echo "Building Java project..."
          ./mvnw clean compile -DskipTests

      # Step 6: OWASP Dependency-Check for Vulnerabilities
      - name: Run OWASP Dependency-Check
        run: |
          echo "Running OWASP Dependency-Check..."
          ./mvnw org.owasp:dependency-check-maven:check -Dformat=ALL
        continue-on-error: true  # Allow workflow to continue even if vulnerabilities are found

      # Step 7: (Optional) Trivy Security Scanner (Application-level & Dependency scan)
      - name: Run Trivy Security Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          ignore-unfixed: true
          severity: "CRITICAL,HIGH"
        continue-on-error: true  # Allow workflow to continue even if vulnerabilities are found

      # Step 8: (Optional) CodeQL Manual Setup (For private repo)
      - name: Setup CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: "java"

      - name: Run CodeQL Analysis
        uses: github/codeql-action/analyze@v2
